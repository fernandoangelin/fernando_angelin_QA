---
default: --format pretty -p html_report -p chrome -p dev

html_report: --format progress --format html --out=features_report.html

firefox: BROWSER=firefox
chrome: BROWSER=chrome

dev: ENVIRONMENT_TYPE=dev
